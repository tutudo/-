---
- name: Install and start Nginx
  hosts: 192.168.1.9  # 指定你的主机组

  tasks:
    - name: Install Nginx
      yum:
        name: nginx
        state: present

    - name: Start Nginx
      systemd:
        name: nginx
        state: started
        enabled: yes
